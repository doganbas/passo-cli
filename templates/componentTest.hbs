import React from 'react';
import 'react-native';
import renderer from 'react-test-renderer';

import {{name}} from '../index';

it('render {{nameLowerCase}}', () => {
  renderer.create(<{{name}} testID={'{{nameLowerCase}}-test'} />);
});

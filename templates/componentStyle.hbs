import {StyleSheet} from 'react-native';

import {fontPixel, heightPixel, screenHeight, screenWidth, widthPixel} from 'helpers/sizeHelper';
import {RGBToRGBA, boxShadow, projectColors, textFont} from 'helpers/stylesHelper';

export const {{nameLowerCase}}Style = StyleSheet.create({
  container: {
    //
  },
});

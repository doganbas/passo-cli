import {ComponentMeta, ComponentStory} from '@storybook/react';
import React from 'react';

import {{name}} from '../index';

const defaultProps: ComponentMeta<typeof {{name}}> = {
  title: '{{componentType}}/{{name}}s',
  component: {{name}},
  parameters: {
    docs: {
      description: {
        component: '{{name}},{{componentType}} içerisinde bulunan bir componenttir.'
      }
    }
  },
  argTypes: {
    testID: {
      description: "Testler için verilecek id'dir.",
      table: {
        category: 'Events'
      }
    },
  }
};

const Template: ComponentStory<typeof {{name}}> = args => <{{name}} {...args} />;

export const Basic{{name}} = Template.bind({});

Basic{{name}}.args = {
  testID: 'basic-{{name}}',
};
